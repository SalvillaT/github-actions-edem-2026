jobs:
  say_hello:
    name: "Lets great otra vez"
    runs-on: ubuntu-latest
    steps:
      - id: get-time
        name: Grab the current time
        run: |
          curTime=$(date)
          echo "currenttime=$(ourTime)" >> $GITHUB_OUTPUT
      - run: echo "The time from the early step is ${{steps.get-time.outputs.currenttime}}"